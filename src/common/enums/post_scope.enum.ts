export enum PostScope {
  DEPARTMENT = 'DEPARTMENT',   // Visible to specific department
  YEAR = 'YEAR',               // Visible to specific year (1st, 2nd, etc.)
  GLOBAL = 'GLOBAL',           // Visible to everyone
}
